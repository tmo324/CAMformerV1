# Unsupported top-k prototypes

These files are retained only to preserve the design history associated with
the paper. Both instantiate an unavailable `fifo` module, and their partition
control logic is incomplete. They are excluded from all release checks.

Do not use either module as a verified top-k implementation.
